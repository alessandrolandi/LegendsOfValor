package items;

/**
 * Enum representing different types of spells.
 * Each spell type has a unique effect on monsters in combat.
 */
public enum SpellType {
    ICE,      // Reduces damage
    FIRE,     // Reduces defense
    LIGHTNING // Reduces dodge chance
}
