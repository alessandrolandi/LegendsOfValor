package items;

/**
 * Enum representing different types of potions.
 * Each type affects a specific hero attribute or resource.
 */
public enum PotionType {
    HEALTH, MANA, STRENGTH, DEXTERITY, AGILITY
}